//sort: 5,7,-1,0,2
//output: -1, 0 , 2, 5, 7

/*
print an array of random nombers
sort alg.

~ most of the numeric alg problems 
  can be solved with the following
  equation:

  class(X);

  main(X);

  Interestingly, the same method 
  is not usually applied with JS.
  It uses the `exports.modules` 
  method but it doesn't follow
  the class method.

sample-code:

arr = [5,7,0];

while(arr > arr.length){
    for()
}



I have also seen the Array.isArray(). 

It is a method to deduce whether the passed parameter is 
an array or not.

arr.sort() is an in-built method to perform sort.
*/

const arr = [5,7,0];

//arr.sort()

// while(arr < arr.length){
//     for(i=0; i< arr.length; i++){
//         if(arr[i] > arr[i+1]){
//             arr[i] == arr[i+1];
//             arr[i+1] == arr[i];
//         }
//         return arr;
//     }
// }

console.log(arr.sort());

// while(arr < arr.length){
//     console.log("get out");
// }

return 0;

