//sort: 5,7,-1,0,2
//output: -1, 0 , 2, 5, 7

//case01: use with sort arr:

//the use of built in methods
//make it easy to implement.
//But the logic is not explained.

const arr = [5,7,-1,0,2];
sorts = arr.sort();

console.log("sorts" + sorts);
ports = arr.pop();
console.log("pop" + ports);