const farenheit = 65

let celsius = ((farenheit -  32) * 5) / 9

/*
* the JS has two string concatenation 
  the comma concatenates string with 1 or some
  space in middle. + sign is to join the two strings
*/

console.log("the temperature today is:" ,celsius + "C");